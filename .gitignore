# Build artifacts (the release zip is attached to GitHub Releases, not committed)
/FS25_NoResetMP.zip
/dist/

# Python / tooling caches
.ruff_cache/
__pycache__/
*.pyc

# Editor / OS
.vscode/
.idea/
Thumbs.db
.DS_Store

# --- Secrets / PII (NEVER commit) ---
.env
.env.*
*.pem
*.key
id_rsa*
*.pfx
Local State
Cookies
